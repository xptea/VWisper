{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window",
  "windows": ["main", "wave-window"],
  "permissions": [
    "core:default",
    "opener:default"
  ]
}
